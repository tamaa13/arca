# Foundry build artifacts
out/
cache/
# Vendored deps (re-fetch with: git clone --depth 1 https://github.com/foundry-rs/forge-std lib/forge-std)
lib/
# Broadcast logs (may contain deployment metadata)
broadcast/
