// Server-safe constant — imported by both ThemeProvider ('use client') and
// ThemeScript (rendered into <head>). Plain export so Next inlines the string.
export const THEME_STORAGE_KEY = "arca-theme";
